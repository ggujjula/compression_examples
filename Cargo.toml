[package]
name = "compression_examples"
version = "0.1.0"
edition = "2024"

[dependencies]
bitvec = { version = "1.0.1", features = ["serde"] }
rmp-serde = "1.3.0"
serde = { version = "1.0.219", features = ["serde_derive"] }

[dev-dependencies]
rand = "0.9.1"
